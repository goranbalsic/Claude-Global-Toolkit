# sources/

Original reference material, kept verbatim. Do not edit files in this
directory — if source content changes, add a new dated entry to
`../SOURCE_REGISTER.md` instead.

## Contents

- `Claude_Global_Toolkit_AIO_Master_Prompt_v2.1.pdf` — the reference PDF this
  entire toolkit was generated from (SRC-001 in `../SOURCE_REGISTER.md`). Its
  own front matter states it is a reference export and that Markdown and
  repository source files are authoritative over it.
- `update.txt` — a domain-agnostic memory/context/prompt/decision-management
  system prompt (SRC-002 in `../SOURCE_REGISTER.md`), supplied to extend
  this repository with `PROJECT_CONTEXT.md`, `PROJECT_RULES.md`,
  `PROMPTS.md`, `IDEAS.md`, `OPEN_QUESTIONS.md`, `memory/`, and
  `session_logs/`. Note: this file is UTF-16 encoded, unlike every other
  text file in this repository — see the SRC-002 entry in
  `../SOURCE_REGISTER.md` for detail.
